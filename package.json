{
  "name": "annotate-for-llm",
  "version": "1.0.0",
  "description": "Load any public web page, annotate specific spans of text, and export the notes in a format an LLM understands. Static PWA + a serverless fetch proxy.",
  "license": "MIT",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch",
    "preview": "wrangler pages dev public",
    "deploy": "npm run build && wrangler pages deploy public",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@mozilla/readability": "^0.6.0",
    "dompurify": "^3.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.0.0",
    "esbuild": "^0.25.0",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
